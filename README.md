# LLM Simulator

# Setup

1. Clone the repo

```bash

git clone https://github.com/alien45/test.git
```

2. Start both frontend and backend using docker containers.

Prereuisites: `docker` and `docker-compose`

```bash
docker-compose up --build
```

2. Open the App UI: http://localhost
